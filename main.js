const form = document.querySelector('#form')
const fileInput = document.querySelector('#file-input')

function runForm () {
    fetch('localhost:3000/uploads').then()
}

form.addEventListener('submit', runForm)